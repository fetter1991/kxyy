# 修改日志

> 记录每次迭代的版本号、日期、修改项目与修改内容。
> 状态图例：✅ 测试通过 ｜ ❌ 测试未通过 ｜ ⏳ 待测试

---

## v1.1.0 — 2026-08-04

**迭代目标**：执行「执行路线图」🟢 第一梯队任务（1~8），修复已知 BUG 与样式偏差。

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 1 | 修复进度条回跳 BUG | V5 / B2 | 进度条 click 事件中同步 seek 媒体元素 `currentTime`，并对 ratio 做 0~1 钳制，避免 `setInterval` 每秒把进度追回原位 | `js/main.js` | ⏳ |
| 2 | 修复列表选中样式 BUG | V4 / B1 | 新增 `refreshPlaylistActive()` 增量刷新选中态替代全量重绘：main.js 中目标项未渲染时自动补渲染并滚动到可视区；nav-player.js 中仅切换 `.playing` 类与播放指示器，保留 scrollTop | `js/main.js`、`js/nav-player.js` | ⏳ |
| 3 | 统一滚动条样式 | G2 | 全局滚动条规则扩展到 `html/body` 与通配符选择器，补齐 Firefox `scrollbar-width/color`；移除 `.nav-playlist-body` 的 3px 局部覆盖；显式声明各弹窗、灯箱、子页面滚动容器 | `css/style.css` | ⏳ |
| 4 | 作品列表每行 4 个 + 统一高度 | A1 | `.works-grid` 改为 `repeat(4, 1fr)` 并加 `grid-auto-rows: 1fr`；`.work-card` 改 flex 纵向布局撑满单元格，`.work-body` 弹性填充、`.work-meta` 用 `margin-top:auto` 底部对齐 | `css/style.css` | ⏳ |
| 5 | 封面比例改为 9:16 | A2 | `.work-cover` 的 `aspect-ratio` 由 `4/3` 改为 `9/16`，并加 `flex-shrink: 0` 防止压缩 | `css/style.css` | ⏳ |
| 6 | 验证横竖屏自动适配 | V3 | 代码侧已实现（CSS `.orientation-landscape`/`.orientation-portrait` + `loadTrack()` 切换 class），本次未改动，待实测确认 | — | ⏳ |
| 7 | 清理 musicData 的 `type` 字段 | D4 | **暂缓执行**：`main.js` 多处依赖 `track.type === 'video'` 分支且 videoData 仍在使用，待确认后统一处理 | — | — |
| 8 | avatar 路径规范化 + 兜底默认图 | D5 / D6 | data.js 中 7 条 musicData 的 avatar 统一改为 `../img/avatar/{artist}.png`；nav-player.js 新增 `resolveAvatar()` 按页面层级解析路径，并绑定 `error` 事件在图片缺失时回退 `VA.png`（带一次性标记防死循环） | `js/data.js`、`js/nav-player.js` | ⏳ |

### 备注

- 任务 8 一并完成了第二梯队的 D5（avatar 路径规范化），因其与 D6 兜底逻辑强耦合。
- 当前 `img/avatar/` 下仅有 `VA.png`、`张震岳.png`、`宋冬野.png`，其余歌手头像将自动回退到 `VA.png`。
- 任务 4 与 5 组合后卡片高度较大（4 列 + 9:16 竖版封面），如实测视觉过高可后续收窄 `.works-grid` 最大宽度。

---

## v1.1.1 — 2026-08-04（第一梯队测试反馈修复）

> 根据大屏/多浏览器实测结果修复：任务 2、3 初测未通过。

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 2-补 | 修复大屏播放列表选中态 | V4 / B1 | 大屏视频页（`pages/music.html`）使用 `js/music.js` 而非 `main.js`，其 `_loadVideoTrack()` 在滚动补渲染后调用全量 `_renderVideoPlaylist()` 不会更新已渲染旧项的 `.playing`，导致选中态残留/不更新。新增 `refreshVideoPlaylistActive()`：仅切换 `.playing` 类，目标项未渲染时循环补渲染并滚动到可视区 | `js/music.js` | ✅ |
| 3-补 | 滚动条全站统一（无背景色 + 滑块高 120px） | G2 | 原方案大屏下未生效（body 滚动条伪元素未接管、弹窗被浏览器默认样式覆盖）。改为：给 `html` 加 `overflow-y: scroll`；用具体类选择器 + `!important` 强制覆盖主页面/子页面/弹窗/灯箱（含 `.work-modal-body`、`.growth-modal-body`、`.nav-playlist-body`、`.music-playlist`、`#pageContainer` 等）；轨道 `background: transparent`（无背景色）、宽度保持 6px、滑块 `min-height: 120px` 固定高度 | `css/style.css` | ⏳ |
| 3-再补 | 消除双滚动条 | G2 | `html { overflow-y: scroll }` + `body { overflow-y: hidden }` 仍出现双滚动条（html 自定义槽 + body 默认滚条并存）。改为标准单滚动容器方案：`html { overflow: hidden; height: 100% }` + `body { overflow-y: auto; height: 100% }`，body 作为唯一根滚动容器，彻底消除双滚动条 | `css/style.css` | ✅ |

---

## v1.1.2 — 2026-08-04（第一梯队 9、10 项）

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 9 | 成长历程滚动条吸附对齐 | 新增 | `.growth-fullscreen-scroll` 已有 `scroll-snap-type: y mandatory`，但子项 `.growth-fullscreen-item` 缺 `scroll-snap-align`，导致拖动时半截卡在两模块间。补加 `scroll-snap-align: start` + `scroll-snap-stop: always`，内容模块完整吸附到视口（大屏/手机均生效） | `css/style.css` | ✅ |
| 10 | 作品页封面图完整显示 | 新增/A2 | 保持 `.works-grid` 4 列，封面图按图片宽度完整显示；并同步优化：圆角 20px→10px、`object-position: top` 消除顶部空白、`.work-body` 上内边距 20px→12px 缩小封面到标题距离；**去掉 `.work-cover` 的 `aspect-ratio: 9/16` 强制比例，封面高度由图片真实宽高比决定（`img` 改 `height: auto`）** | `css/style.css` | ✅ |

### 备注

- 任务 9 的 `scroll-snap-type` 父容器规则早已存在，根因是子项未声明 `scroll-snap-align`，本次补齐即完成吸附。
- 任务 10 的完整显示改为 `contain` + `object-position: top`，既保留图片完整，又避免顶部留白；圆角与间距微调均不影响 A1/A2 的网格布局与 9:16 比例。

### 备注

- 任务 2 小屏/手机模式原本正常（走 `main.js`/`nav-player.js` 的增量刷新），本次仅补齐大屏 `music.js` 路径。
- 任务 3 的 `!important` 仅作用于滚动条伪元素，不影响 `.growth-fullscreen-scroll` / `.growth-timeline-nav` 等原有 `display:none` 的隐藏滚动条规则（不在具体类列表中）。

---

## v1.1.3 — 2026-08-04（第二梯队资源迁移 9~19）

> 执行「执行路线图」🟡 第二梯队资源迁移与数据调整（R1~R9）。已跟踪文件用 `git mv` 保留历史，未跟踪媒体文件用 `Move-Item` 迁移。

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 9 | videoData 去 `artist`、加 `desc` | D3 | `videoData` 移除 `artist` 字段、新增 `desc`；播放器 UP 主名统一显示常量 `开心元元` | `js/data.js`、`js/music.js` | ✅ |
| 11 | 资源图片迁移（avatar） | R5 | `img/avatar/{VA,宋冬野,张震岳}.png` → `assets/img/avatar/`，`data.js` 头像路径已为 `../assets/img/avatar/` | `assets/img/avatar/` | ✅ |
| 12 | 资源图片迁移（global） | R2 | `bg.png` `logo.png` `loading.gif` `favicon.ico` `feather0~3.png` `pattern.svg` → `assets/img/global/`；CSS body 背景 `url(../assets/img/global/bg.png)`；`feathers.js` 羽毛图路径改为 `assets/img/global/featherX.png`；各 html `favicon/loading/logo` 引用同步更新 | `assets/css/style.css`、`assets/js/feathers.js`、`index.html`、`pages/*.html` | ✅ |
| 13 | 资源图片迁移（works） | R3 | `img/00.jpg~21.jpg` → `assets/img/works/`；`galleryData`（及 works/growth 复用项）`url` 改为 `../assets/img/works/0X.jpg`；`profile.html` 头像引用改为 `../assets/img/works/00.jpg` | `assets/js/data.js`、`pages/profile.html` | ✅ |
| 14 | 资源图片迁移（growth） | R4 | `growth.js` 中 `../img/pattern.svg` → `../assets/img/global/pattern.svg`；`assets/img/growth/` 目录预留（成长历程图当前复用 works 资源） | `assets/js/growth.js` | ✅ |
| 16 | 迁移 `css/` → `assets/css/` | R6 | `css/style.css` → `assets/css/style.css`，所有 html `<link>` 引用改为 `assets/css/`（子页面 `../assets/css/`） | `index.html`、`pages/*.html`、`assets/css/` | ✅ |
| 17 | 迁移 `js/` → `assets/js/` | R7 | `js/*.js` → `assets/js/`，所有 html `<script>` 引用改为 `assets/js/`（子页面 `../assets/js/`） | `index.html`、`pages/*.html`、`assets/js/` | ✅ |
| 18 | 迁移 `video/` → `assets/video/` | R8 | `video/*.mp4` → `assets/video/`；`data.js` `videoUrl` 改为 `../assets/video/...` | `assets/js/data.js`、`assets/video/` | ✅ |
| 19 | 迁移 `music/` → `assets/music/` | R9 | `music/*` → `assets/music/`；`data.js` `audioUrl` 改为 `../assets/music/...`（修正马马嘟嘟骑误写的 `../assets/video/` 路径） | `assets/js/data.js`、`assets/music/` | ✅ |

### 备注

- 任务 10（D5 avatar 路径）早于第一梯队任务 8 完成，未重复执行。
- 任务 15（PC 数字分页，V2）属功能新增，留待第三梯队冲刺，本次未做。
- 迁移后根目录已无 `css/`、`js/`、`img/`、`music/`、`video/` 旧目录；媒体文件因未纳入 git 跟踪，采用移动命令迁移，引用路径已全量更新。
- `data.js` 中部分歌手头像（Sasablue/王菲/周深/鞠婧祎/郭斯与帆）对应 `png` 文件原始即缺失，将回退到 `VA.png`（D6 兜底逻辑已覆盖）。

---

## v1.1.4 — 2026-08-04（资源迁移路径错误修复）

> 修复 v1.1.3 资源迁移后图片显示错误：背景图、`feather0~3.png`、歌手头像、`VA.png` 均报 404。
> 根因：迁移后路径前缀计算错位（CSS `url()` 相对 CSS 文件位置、JS 工具按页面层级解析基准目录，二者规则不一致导致部分资源路径多/少一层 `assets/`）。

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 12-补 | 修复背景图路径 | R2 | CSS `body` 背景 `url('../assets/img/global/bg.png')` 改为 `url('../img/global/bg.png')`：CSS 位于 `assets/css/`，`..` 已回到 `assets/`，原写法会多拼一层 `assets/` 导致 `assets/assets/img/...` 404 | `assets/css/style.css` | ✅ |
| 12-补 | 修复羽毛图路径 | R2 | `feathers.js` 的 `detectBasePath()` 改为按页面层级返回前缀（首页 `''`、子页面 `'../'`），不再从 `script src` 截取 `assets/`，避免子页面拼出 `../assets/assets/img/...` 重复层级 | `assets/js/feathers.js` | ✅ |
| 12-补 | 修复头像基准目录 | R5 | `nav-player.js` 的 `AVATAR_BASE` 由 `img/avatar/`（首页）/ `../img/avatar/`（子页）改为 `assets/img/avatar/`（首页）/ `../assets/img/avatar/`（子页），修正 404 | `assets/js/nav-player.js` | ✅ |
| 14-补 | 修复成长历程装饰图 | R4 | `main.js` 渲染成长历程时间线时 `../img/pattern.svg` 改为 `../assets/img/global/pattern.svg`（实际渲染在 main.js，growth.js 中的同名引用此前已改） | `assets/js/main.js` | ✅ |

### 备注

- 全量扫描确认：除 `style.css:32`（`../img/global/bg.png`，相对 CSS 解析正确）与 `nav-player.js:131`（注释）外，全站已无残留旧 `img/` 引用。
- HTML 中 favicon/loading/logo 引用（首页 `assets/img/global/`、子页 `../assets/img/global/`）均正确，未受影响。
- 歌手头像（Sasablue/王菲/周深/鞠婧祎/郭斯与帆）物理文件仍缺失，由 D6 兜底回退 `VA.png`，现已可正常显示。

---

## v1.1.5 — 2026-08-04（头像路径补强）

> 用户反馈头像、VA.png 仍报 404（实际请求路径少了 `assets/` 一层）。将头像解析改为直接规范化 `data.js` 完整路径，消除拼接基准目录可能缺失层级的风险。

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 12-补 | 头像路径规范化 | R5 | `nav-player.js` 弃用 `AVATAR_BASE + fileName` 拼接方式，改为 `normalizeAvatarPath()` 直接基于 `data.js` 的 `../assets/img/avatar/{name}.png` 按页面层级补全前缀（首页去 `../`，子页保留 `../`） | `assets/js/nav-player.js` | ✅ |

### 备注

- 首页：`../assets/img/avatar/X.png` → `assets/img/avatar/X.png`；子页：保持 `../assets/img/avatar/X.png`。两层均显式包含 `assets/`，避免缺层。
- 回退 `VA.png` 同样经 `normalizeAvatarPath` 处理，确保兜底路径正确。


---

## v1.1.6 — 2026-08-04（视频播放器专辑 Tab + PC 分页）

> 执行「第二梯队」任务 15 与「第三梯队」任务 20：music.html 视频播放器新增专辑 Tab，PC 端组合数字分页，小屏保留滚动加载。

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 15 | PC 端视频列表数字分页 | V2 | ≥1024px 下列表改用数字胶囊分页（每页 8 条），与专辑 Tab 组合；分页含上一页/下一页/省略号；小屏（<1024px）关闭分页、保留「加载更多」滚动加载 | `assets/js/music.js`、`assets/css/style.css` | ✅ |
| 20 | 视频播放器 Tab 专辑切换 | V1 | ① `data.js` 中 `videoData` 扁平数组改为 `videoAlbums` 分专辑结构（`[{name, videos:[...]}]`），按现有视频分类打标为「作品集/日常记录/国风写真」；② music.html 新增 `album-tab-bar`；③ Tab 可扩展，仅当横向溢出时显示左右箭头；④ 切换专辑重渲染列表，已播放视频继续播放，若不在新列表则 `.playing` 高亮消失 | `assets/js/data.js`、`assets/js/music.js`、`pages/music.html`、`assets/css/style.css` | ✅ |

### 备注

- 专辑名称仅为初始示意，后续可按实际视频分类增删（数据驱动，无需改结构）。
- PC 端交互 = 专辑 Tab（顶部）+ 数字分页（底部）；小屏交互 = 专辑 Tab（顶部）+ 滚动加载「加载更多」。
- 切换专辑时音视频不中断，仅列表高亮随所属专辑是否存在当前播放项而保留/消失。


### UI 样式迭代（参考截图）

| 序号 | 修改项目 | 对应条目 | 修改内容 | 涉及文件 | 状态 |
|------|---------|---------|---------|---------|------|
| 20-补 | 播放列表样式参考 ihan.com.cn 音乐页 | V1 | 列表项改为「序号/播放图标 + 固定 56×56 小方图 + 单行标题」布局；每项底部加细分割线；hover 显示播放按钮；当前播放项序号/图标使用青色（#40e0d0）高亮 | `assets/js/music.js`、`assets/css/style.css` | ✅ |
| 15-补 | 专辑 Tab 样式调整 | V2 | Tab 栏改为深色圆角胶囊容器，active 为白色高亮、inactive 为半透明白字；右侧增加筛选/菜单按钮 | `pages/music.html`、`assets/css/style.css` | ✅ |

### 备注补充

- 列表项仍与顶部播放器保持耦合：点击任意列表项会把对应视频加载到播放器并播放，同时刷新列表高亮。
- 专辑菜单按钮当前为视觉占位，如需展开专辑选择浮层可后续扩展。
- 固定小图通过 `object-fit: cover` 保证不变形，容器 `56px × 56px`。

### 样式问题修复

| 序号 | 问题 | 修复 | 涉及文件 |
|------|------|------|---------|
| 1 | 小屏切换专辑 / 点击列表项时出现白色底部阴影 | 为 `.album-tab`、`.album-tab-arrow`、`.album-tab-menu`、`.playlist-item`、`.page-btn`、`.playlist-load-more` 添加 `-webkit-tap-highlight-color: transparent` 并去除 `:active` 的 `box-shadow`；active 标签去掉 `box-shadow` | `assets/css/style.css` |
| 2 | 列表序号紧贴左侧不美观 | `.playlist-item` 增加左右内边距（10px 8px），`.pl-index` / `.pl-play-icon` 增加 `margin-left: 4px`，`.pl-info` 增加 `padding-left: 4px` | `assets/css/style.css` |
| 3 | 播放器、专辑、列表为三个独立部分 | 在 `music.html` 中将三者包裹进统一的 `.music-panel` 卡片（半透明深色背景 + 圆角 + 边框 + 阴影），三者合为同一面板 | `pages/music.html`、`assets/css/style.css` |
| 4 | 播放器失效（music.js 元素 ID 与页面不符） | 修正 `cacheElements` 映射为真实 ID（`playBtn`/`prevBtn`/`nextBtn`/`fullscreenBtn`/`videoContainer`/`progressBar`/`progressCurrent`/`currentTime`/`totalTime`），进度与时间显示跟随真实结构 | `assets/js/music.js` |
